spring:
  application:
    name: gateway-service
  cloud:
    config:
      uri: http://config-service:8888
      fail-fast: true
      retry:
        max-attempts: 10
        max-interval: 2000
    discovery:
      enabled: true
